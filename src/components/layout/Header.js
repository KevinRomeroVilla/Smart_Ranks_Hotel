import "./Header.css";

const Header = () => {
  return (
    <header id="header" className="header">
      <div>Smart Ranks Hotel</div>
      <nav>
        <button>Login</button>
      </nav>
    </header>
  );
};

export default Header;
